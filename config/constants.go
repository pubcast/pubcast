package config

// These are the testing defaults, not a good place to keep things
const (
	// ServerHostname is the domain name of the server
	// Example: "bestpodcasts.com"
	ServerHostname = "server.hostname"

	// ServerPort is the port number of the server
	// Example: 8080
	ServerPort = "server.port"

	// UploadLocation is where the podcast files are stored
	UploadLocation = "~/.pubcast"
)
